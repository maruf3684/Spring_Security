server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/spring_security
    username: munna
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true